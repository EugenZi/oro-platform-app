bap_bundle:
    resource:     "@BapIssueBundle/Controller"
    type:         annotation
    prefix:       /bap_

bap_api:
    resource:     "@BapIssueBundle/Resources/config/oro/routing.yml"
    type: rest
    prefix:       api/rest/{version}
    requirements:
        version:  latest|v1
    defaults:
        version:  latest
