{% if resource_granted('bts_issue_view') %}
    {{ oro_widget_render({
        'widgetType': 'block',
        'url': path('bap_issue_user_issues', {'id': entity.id}),
        'title': 'bap_issue.issue.assigned_issues'|trans
    }) }}
{% endif %}
