{% if resource_granted('bts_issue_view') %}
    {{ oro_widget_render({
        'widgetType': 'block',
        'url': path('bap_user_issues', {'id': entity.id}),
        'title': 'bap.issue.assigned_issues'|trans
    }) }}
{% endif %}
