{% extends 'OroUIBundle:actions:update.html.twig' %}
{% form_theme form with 'OroFormBundle:Form:fields.html.twig' %}

{% if form.vars.value.id %}
    {% oro_title_set({params : {"%entity.title%": form.vars.value.title} }) %}
{% endif %}
{% set formAction = form.vars.value.id ? path('bts_resolution_update', { id: form.vars.value.id }) : path('bts_resolution_create') %}

{% block navButtons %}
    {% if form.vars.value.id and resource_granted('DELETE', form.vars.value) %}
        {{ UI.deleteButton({
            'dataUrl': path('bts_api_delete_resolution', {'id': form.vars.value.id}),
            'dataRedirect': path('bts_resolution_index'),
            'aCss': 'no-hash remove-button',
            'id': 'btn-remove-resolution',
            'dataId': form.vars.value.id,
            'entity_label': 'academic.bts.resolution.entity_label'|trans
        }) }}
        {{ UI.buttonSeparator() }}
    {% endif %}
    {{ UI.cancelButton(path('bts_resolution_index')) }}
    {{ UI.saveAndCloseButton('Save'|trans) }}
{% endblock navButtons %}

{% block pageHeader %}
    {% if form.vars.value.id %}
        {% set breadcrumbs = {
            'entity':      form.vars.value,
            'indexPath':   path('bts_resolution_index'),
            'indexLabel': 'academic.bts.resolution.entity_plural_label'|trans,
            'entityTitle': form.vars.value.title|default('N/A')
        } %}
        {{ parent() }}
    {% else %}
        {% set title = 'oro.ui.create_entity'|trans({'%entityName%': 'academic.bts.resolution.entity_label'|trans}) %}
        {% include 'OroUIBundle::page_title_block.html.twig' with { title: title } %}
    {% endif %}
{% endblock pageHeader %}

{% block content_data %}
    {% set id = 'resolution-edit' %}

    {% set dataBlocks = [{
        'title': 'General'|trans,
        'class': 'active',
            'subblocks': [{
                'title': '',
                'data': [
                    form_row(form.title)
                ]
            }]
        }]
    %}
    {% set data =
        {
            'formErrors': form_errors(form)? form_errors(form) : null,
            'dataBlocks': dataBlocks,
        }
    %}
    {{ parent() }}
{% endblock content_data %}
