{% extends 'OroDashboardBundle:Dashboard:widget.html.twig' %}
{% import 'OroDataGridBundle::macros.html.twig' as dataGrid %}

{% block content %}
    {{ dataGrid.renderGrid('dashboard-issue-grid', {'userId': user.id}) }}
{% endblock %}

{% block actions %}
    {% set actions = [
        {
            'type': 'link',
            'label': 'academic.bts.dashboard.widget.view_issues_page'|trans,
            'url': path('bts_issue_index', {
                'grid[issue-grid]': {
                    'f[collaborators][value]': app.user.id,
                    'f[collaborators][type]': 1
                }|url_encode
            })
        }
    ] %}

    {{ parent() }}
{% endblock %}
